define(['Vendor'],function(Vendor){
    
    'use strict';

    var _          = Vendor._,
        Backbone   = Vendor.backbone,
        selectedListItemsCollection;

    selectedListItemsCollection = Backbone.Collection.extend({

    });

 return selectedListItemsCollection;
});